spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jobanalytics
    username: admin
    password: admin123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080
